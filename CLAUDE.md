# Project Rules

## Git Workflow

- **Every new feature gets its own branch.** Never develop a new feature directly on an existing branch. Create a descriptive branch (e.g. `feature/ably-spaces`) before starting work.
- Commit messages should be clear and describe the *why*, not just the *what*.
- Run tests before committing.
